apiVersion: v1
kind: Pod
metadata:
  name: nginx
  #namespace: roboshop
  labels:
    project: roboshop
    env: dev
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports:
    - containerPort: 80
