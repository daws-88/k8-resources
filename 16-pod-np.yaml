apiVersion: v1
kind: pod
metadata:
  name: service-np
  labels:   # These labels are used to register this pod with service
      project: roboshop
      component: frontend
      environment: qa
      purpose: study
spec:
   containers:
   - name: nginx
     image: nginx