apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: roboshop-ebs
reclaimPolicy: Retain ## default is true
provisioner: ebs.csi.aws.com
volumeBindingMode: WaitForFirstConsumer
## stroage classesc is created by admins
